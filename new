# Step 2A: Check Employee Master date coverage

date_columns = [
    "Actual Join Date",
    "Group Join Date",
    "Resignation Date"
]

for col in date_columns:
    print(f"\n========== {col} ==========")
    
    if col in employee_master.columns:
        dates = pd.to_datetime(employee_master[col], errors="coerce")
        
        print("Valid dates:", dates.notna().sum())
        print("Missing dates:", dates.isna().sum())
        print("Minimum:", dates.min())
        print("Maximum:", dates.max())
    else:
        print("Column not found")
