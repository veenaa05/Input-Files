invalid_lwd_summary = (
    invalid_lwd
    .groupby(['Resignation Status', 'Resignation Type'])
    .size()
    .reset_index(name='records')
    .sort_values('records', ascending=False)
)

display(invalid_lwd_summary)



/////////////
print("Invalid LWD records:", len(invalid_lwd))
print("Total approved unique employees:", len(valid_resignations))

print(
    "Invalid %:",
    round(len(invalid_lwd) / len(valid_resignations) * 100, 2),
    "%"
)
