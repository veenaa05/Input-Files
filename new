# Prepare data for Prophet

prophet_data = monthly_headcount[['Month', 'Headcount']].copy()

prophet_data = prophet_data.rename(
    columns={
        'Month': 'ds',
        'Headcount': 'y'
    }
)

prophet_data['ds'] = pd.to_datetime(prophet_data['ds'])
prophet_data['y'] = prophet_data['y'].astype(float)

print("Prophet data shape:", prophet_data.shape)
display(prophet_data.tail(10))


# Train-test split

test_months = 6

train = prophet_data.iloc[:-test_months].copy()
test = prophet_data.iloc[-test_months:].copy()

print("Training records:", len(train))
print("Testing records:", len(test))

print("\nTraining period:")
print(train['ds'].min(), "to", train['ds'].max())

print("\nTesting period:")
print(test['ds'].min(), "to", test['ds'].max())




from prophet import Prophet

model = Prophet(
    yearly_seasonality=True,
    weekly_seasonality=False,
    daily_seasonality=False
)

model.fit(train)


