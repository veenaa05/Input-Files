df["Date Of Joined"] = pd.to_datetime(
    df["Date Of Joined"],
    errors="coerce"
)

print("Valid joining dates:", df["Date Of Joined"].notna().sum())
print("Invalid/missing dates:", df["Date Of Joined"].isna().sum())

print("\nDate range:")
print("Minimum:", df["Date Of Joined"].min())
print("Maximum:", df["Date Of Joined"].max())



//////
print(df["Employee Status"].value_counts(dropna=False))


//////////
monthly_joiners = (
    df.dropna(subset=["Date Of Joined"])
      .set_index("Date Of Joined")
      .resample("MS")
      .size()
      .reset_index(name="new_joiners")
)

print(monthly_joiners.head())
print("\nMonthly dataset shape:", monthly_joiners.shape)
