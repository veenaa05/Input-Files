# Check Employee Master dates required for historical headcount

employee_master['Actual Join Date'] = pd.to_datetime(
    employee_master['Actual Join Date'], errors='coerce'
)

employee_master['Last Working Date'] = pd.to_datetime(
    employee_master['Last Working Date'], errors='coerce'
)

print("Total employees:", len(employee_master))

print("\nMissing Actual Join Date:",
      employee_master['Actual Join Date'].isna().sum())

print("Missing Last Working Date:",
      employee_master['Last Working Date'].isna().sum())

print("\nActual Join Date range:")
print(
    employee_master['Actual Join Date'].min(),
    "to",
    employee_master['Actual Join Date'].max()
)

print("\nLast Working Date range:")
print(
    employee_master['Last Working Date'].min(),
    "to",
    employee_master['Last Working Date'].max()
)




# Check employee status distribution

print(employee_master['Employee Status'].value_counts(dropna=False))

