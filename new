# Check employee 10145 in the original OT source

emp = "10145"

print("In ot_source:",
      (ot_source["EMP_CODE_STD"].astype(str).str.strip() == emp).sum())

print("In ot_trend_features:",
      (ot_trend_features["EMP_CODE_STD"].astype(str).str.strip() == emp).sum())

print("\nOT source record:")
print(
    ot_source[
        ot_source["EMP_CODE_STD"].astype(str).str.strip() == emp
    ].to_string(index=False)
)
