print(monthly_joiners.to_string(index=False))


plt.figure(figsize=(12, 5))

plt.plot(
    monthly_joiners["Date Of Joined"],
    monthly_joiners["new_joiners"],
    marker="o"
)

plt.title("Monthly New Joiners")
plt.xlabel("Month")
plt.ylabel("Number of New Joiners")
plt.xticks(rotation=45)
plt.grid(True)

plt.tight_layout()
plt.show()







print("Total new joiners:", monthly_joiners["new_joiners"].sum())
print("Average monthly joiners:", monthly_joiners["new_joiners"].mean())
print("Minimum monthly joiners:", monthly_joiners["new_joiners"].min())
print("Maximum monthly joiners:", monthly_joiners["new_joiners"].max())
